package com.andreidiego.mpfi.stocks.deprecated

import org.scalatest.freespec.AnyFreeSpec

class PurchaseTest extends AnyFreeSpec {
/*
  "A Purchase" - {
    "must know how to calculate its" - {
      val purchase = Purchase("28/08/2017", 'AMAR3, 1500, 7.12)
      "Volume" in {
        assert(purchase.volume == purchase.quantity * purchase.unitPrice)
      }
      "Liquidation Fee" in {
        assert(purchase.liquidationFee == purchase.volume*0.0275%)
      }
      "Emoluments" in {
        assert(purchase.emoluments == purchase.volume*0.005%)
      }
      "Brokerage Fee" in {
        assert(purchase.brokerageFee == 4)
      }
      "Share in the Service Plan" in {
        assert(sale.shareInServicePlan == 4)
      }
      "Total" in {
        assert(purchase.total == purchase.volume + purchase.liquidationFee + purchase.emoluments + purchase.brokerageFee)
      }
    }
  }
*/
}
